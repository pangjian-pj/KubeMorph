---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: reorchestrationplans.core.kubex.io
spec:
  group: core.kubex.io
  names:
    kind: ReOrchestrationPlan
    listKind: ReOrchestrationPlanList
    plural: reorchestrationplans
    shortNames:
    - rop
    singular: reorchestrationplan
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ReOrchestrationPlan is the Schema for the reorchestrationplans
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ReOrchestrationPlanSpec defines the desired state of ReOrchestrationPlan.
            properties:
              moves:
                description: Moves is the list of replica moves.
                items:
                  description: PlanMove defines a single replica move from source
                    to destination.
                  properties:
                    destination:
                      description: Destination location.
                      properties:
                        clusterId:
                          description: ClusterID is the member cluster identifier.
                          minLength: 1
                          type: string
                        clusterName:
                          description: ClusterName is the display name of the member
                            cluster, used by UI.
                          type: string
                        nodeName:
                          description: NodeName is the Kubernetes node name inside
                            that member cluster.
                          minLength: 1
                          type: string
                      type: object
                    globalDeploymentRef:
                      description: GlobalDeploymentRef identifies the target GlobalDeployment.
                      properties:
                        name:
                          description: Name is the name of the referenced object.
                          minLength: 1
                          type: string
                        namespace:
                          description: Namespace is the namespace of the referenced
                            object.
                          minLength: 1
                          type: string
                      required:
                      - name
                      - namespace
                      type: object
                    replicaIndex:
                      description: ReplicaIndex identifies a replica within the GlobalDeployment.
                      format: int32
                      minimum: 0
                      type: integer
                    source:
                      description: Source location.
                      properties:
                        clusterId:
                          description: ClusterID is the member cluster identifier.
                          minLength: 1
                          type: string
                        clusterName:
                          description: ClusterName is the display name of the member
                            cluster, used by UI.
                          type: string
                        nodeName:
                          description: NodeName is the Kubernetes node name inside
                            that member cluster.
                          minLength: 1
                          type: string
                      type: object
                  required:
                  - destination
                  - globalDeploymentRef
                  - source
                  type: object
                type: array
              summary:
                description: Summary is plan summary.
                properties:
                  currentScore:
                    description: CurrentScore is the total score of the current placement.
                    type: number
                  estimatedImprovementScore:
                    description: EstimatedImprovementScore is a high-level improvement
                      score.
                    type: number
                  expectedScore:
                    description: ExpectedScore is the total score of the expected
                      placement.
                    type: number
                  goalScores:
                    additionalProperties:
                      description: GoalScoreSummary describes current/expected score
                        for one goal.
                      properties:
                        currentScore:
                          description: CurrentScore is the score of current placement
                            for this goal.
                          type: number
                        estimatedImprovementScore:
                          description: EstimatedImprovementScore is (currentScore
                            - expectedScore), clamped to >= 0.
                          type: number
                        expectedScore:
                          description: ExpectedScore is the score of expected placement
                            for this goal.
                          type: number
                        humanReadable:
                          description: |-
                            HumanReadable provides optional, goal-specific readable metrics in original units.
                            For example:
                            - Cost: totalCostUsdFrom/To
                            - Latency/Communication: avgLatencyMsFrom/To
                            - Energy: totalPowerWFrom/To
                            - Migration: movesFrom/To
                          properties:
                            delta:
                              description: Delta is (From - To). Positive means improvement.
                              type: number
                            detail:
                              additionalProperties:
                                type: string
                              description: Detail contains optional extra info for
                                UI/debug.
                              type: object
                            from:
                              description: From is the current placement metric value.
                              type: number
                            kind:
                              description: Kind is the goal type, e.g. Cost/Latency/Communication/Energy/Migration.
                              type: string
                            to:
                              description: To is the expected placement metric value.
                              type: number
                            unit:
                              description: Unit is a human-readable unit string, e.g.
                                "USD", "ms", "W", "moves".
                              type: string
                          type: object
                        weight:
                          description: Weight is the goal weight used in OptimizationPolicy.
                          type: number
                      type: object
                    description: |-
                      GoalScores provides per-goal score breakdown for better explainability.

                      Key is goal type, e.g. Cost/Latency/Communication/Energy/Migration.
                      Values follow the same convention as total scores: lower is better.
                    type: object
                  podsToMove:
                    description: PodsToMove is the number of replicas that need to
                      move.
                    format: int32
                    type: integer
                  policyName:
                    description: PolicyName is the OptimizationPolicy name that generated
                      this plan.
                    minLength: 1
                    type: string
                type: object
            required:
            - summary
            type: object
          status:
            description: ReOrchestrationPlanStatus defines the observed state of ReOrchestrationPlan.
            properties:
              completionTime:
                description: CompletionTime records when execution completes.
                format: date-time
                type: string
              conditions:
                description: Conditions provide detailed diagnostics.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              moveStatuses:
                description: MoveStatuses records per-move execution state.
                items:
                  description: MoveStatus records execution progress for a move.
                  properties:
                    globalDeploymentRef:
                      description: GlobalDeploymentRef identifies the target GlobalDeployment.
                      properties:
                        name:
                          description: Name is the name of the referenced object.
                          minLength: 1
                          type: string
                        namespace:
                          description: Namespace is the namespace of the referenced
                            object.
                          minLength: 1
                          type: string
                      required:
                      - name
                      - namespace
                      type: object
                    message:
                      description: Message contains human-readable error / progress
                        message.
                      type: string
                    replicaIndex:
                      description: ReplicaIndex identifies a replica within the GlobalDeployment.
                      format: int32
                      minimum: 0
                      type: integer
                    status:
                      description: Status is the move status.
                      enum:
                      - Pending
                      - InProgress
                      - Succeeded
                      - Failed
                      type: string
                  required:
                  - globalDeploymentRef
                  type: object
                type: array
              phase:
                description: Phase is the main lifecycle status.
                enum:
                - Pending
                - Executing
                - Succeeded
                - Failed
                - PartiallyFailed
                - Terminating
                type: string
              startTime:
                description: StartTime records when execution starts.
                format: date-time
                type: string
              summary:
                description: Summary is execution summary.
                properties:
                  failedMoves:
                    description: FailedMoves is the number of failed moves.
                    format: int32
                    type: integer
                  pendingMoves:
                    description: PendingMoves is the number of pending moves.
                    format: int32
                    type: integer
                  succeededMoves:
                    description: SucceededMoves is the number of succeeded moves.
                    format: int32
                    type: integer
                  totalMoves:
                    description: TotalMoves is the total number of moves.
                    format: int32
                    type: integer
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
